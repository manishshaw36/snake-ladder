import React from "react";

interface Props {
    playerInfo: any;
    activePlayer: number;
}

const PlayerTurn = (props: Props) => {
    const { playerInfo, activePlayer } = props;
    return 
}

export default PlayerTurn;